# Проверка версии для хакатона

Последнее дополнение: **6 сентября 2026**. Исторические проверки ниже относятся к указанным там версиям и окружениям.

## Новая модель и синхронизация main

Установлена модель **`594d4e6509459b08`**: три CatBoost по 600 деревьев глубины 6, learning rate 0,03, L2 20, 78 признаков. На прежних пяти development folds points RMSE — **0,067357**, blocks — **0,093206**, снижение 1,32% и 1,03%. Warm CPU-инференс замедлился на 4–7% относительно уже ускоренной версии. [Обучение и ограничения](../README.md#как-обучалась-текущая-модель), [полный отчёт](analysis/rmse-wave/REPORT.md).

После обучения локальная `main` синхронизирована с `origin/main` **`c9bbb33bad702856f69e928f35b5c14b42c1ca0b`**, а локальные ML-изменения перенесены поверх неё. Удалённая история была переписана и не имела общего предка с прежней локальной веткой. Исходная ветка сохранена как `backup/main-before-sync-20260906`, локальные файлы — в stash; контрольные суммы и журнал синхронизации находятся в `artifacts/git-sync-20260906`. Новые frontend, backend, полевые отчёты и материалы защиты из upstream сохранены.

На объединённом коде, macOS arm64 / Python 3.12.9, с локальными PostGIS/Redis:

- **186 pytest passed**, 14,84 с; 31 предупреждение rasterio/affine.
- Ruff check и format прошли для 96 Python-файлов.
- Исполняемая структура всех десяти ML-модулей совпала с проверенной до синхронизации; изменения upstream в этих местах переводили комментарии и docstrings.
- Веса, manifest и оба новых submission сохранили SHA-256. Повторное построение прогнозов текущим CLI и проверка результата зафиксированы в [post-sync-checks.json](analysis/rmse-wave/post-sync-checks.json).

Frontend и живой спутниковый сбор в ходе этой синхронизации повторно не запускались; Docker-образы после pull не пересобирались. Результаты предыдущего выпуска и его полевых проверок ниже остаются историческими. Прежние test RMSE **0,075034**, полевые RMSE и показатели презентации относятся к **`131aee618934151e`**. Test RMSE и внешнее покрытие интервалов новых весов не измерялись. Текущие файлы: [submission.csv](../deliverables/submission.csv), [submission-new-test.csv](../deliverables/submission-new-test.csv).

После проверки очищены восстанавливаемый кеш матриц, временное `.venv-release`, Python/pytest/Ruff-кеши и дубли резервной копии, уже сохранённой в stash: около 1,1 ГБ. Исходные данные, все промежуточные прогнозы, маски, отчёты, скрипты и версии моделей сохранены. Сверка 583 файлов проекта не обнаружила изменений при очистке. Локальные входные CSV исключены из Git; готовые результаты в `deliverables/` входят в поставку.

## Проверки 5 сентября 2026

Среда: локальный Windows + Linux Docker Compose, PostGIS/Redis, production Next.js, Google Chrome. Машиночитаемые результаты: [verification.json](analysis/release-2026-09-05/verification.json).

## Итог подготовки защиты

После аудита исходных вложений выполнен новый полный прогон. [Машиночитаемая сводка](analysis/defense-release/verification.json) отделяет эти проверки от более раннего релиза ниже.

| Проверка | Итог |
|---|---|
| Python / API / ML | **161 passed**, 99,15 с |
| Frontend unit | **21 passed**, три файла |
| Playwright / Chrome | **93 passed, 7 skipped**, из 100 сценариев; ошибок и flaky нет |
| TypeScript, ESLint, Ruff, форматирование | Прошли |
| Production | Backend и frontend пересобраны; миграции, API, worker, scheduler и frontend запущены |
| Реальные полевые данные | Три контура, 693 спутниковые записи; одинаковые маски M0 и итоговой модели |
| Повторение без сети | Три ряда, события и сводки совпали; метрики пересчитаны из сохранённых целей/прогнозов |
| Сквозной API | Реальный российский контур, Landsat + погода + три исторических сезона; первая попытка, три экспорта и SHA-256 |
| Презентация | 15 слайдов просмотрены после импорта финального PPTX; пять таблиц, три диаграммы с встроенными данными; структурные проверки пройдены |

Семь пропусков Playwright ожидаемы: шесть desktop-сценариев sidebar не применяются к mobile Sheet, один mobile-сценарий не применяется к desktop. Это не семь непроверенных общих функций. Браузерные workflow-тесты используют подмену ответов для воспроизводимости; реальный сбор проверен отдельно. В pytest 31 предупреждение rasterio/affine и одно о недоступной записи кеша тестов, без ошибок тестов.

Сквозной запуск после обновления: российский контур 144,24 га, **20–30 июня 2024**, Landsat, ERA5-Land, история трёх сезонов. `job=succeeded`, `attempt=1`, результат `partial`, два пригодных наблюдаемых дня, одно отклонение, правило сводки v3. Все CSV / GeoJSON / JSON и manifests выгружены и проверены по SHA-256. 40 чтений при concurrency=4: p95 ≈97,18 мс, max ≈120,80 мс; POST ≈726,90 мс. Это локальная проверка работоспособности, не SLA.

Модель, её manifest и оба submission сохранили прежние SHA-256. Нового обучения и подбора по ответам или реальным полевым примерам не выполнялось. [Отчёт](analysis/field-cases/REPORT.md) показывает и улучшения, и ухудшения относительно M0. Презентация проверена программным рендером; открытие в настольном PowerPoint не заявляется.

## Предыдущий релиз: автоматические проверки

| Проверка | Результат | Область |
|---|---|---|
| Python pytest в Linux-контейнере | 146 passed | API, геометрия, изоляция, CSRF, очередь, отмена, провайдеры, ML, manifests, утечки |
| Frontend unit | 21 passed | Формы, даты, графики, сценарий прокрутки |
| Playwright, desktop + mobile | 85 passed, 1 skipped | Навигация, карта, контуры, формы, анализ, экспорт, ошибки, доступность, WebGL fallback |
| TypeScript / ESLint | Прошли | Типы и статический анализ frontend |
| Production build | Docker frontend и backend собраны | Фактические образы локального приложения |
| OpenAPI / миграции | Схема обновлена и проверена; миграции не требуются | Совместимость serializer, контракта и TypeScript |
| Submission через Docker | 3 112 строк, checksum совпал | Автономный инференс и валидация формата |

На mobile пропущен desktop-сценарий сворачивания sidebar: на телефоне используется отдельный Sheet. Провайдерские тесты используют fixtures и синтетические/записанные растры. Это не заменяет отдельную проверку живых источников. В pytest остаются 31 предупреждение о будущих изменениях rasterio/affine, без ошибок.

## Исправление прогресса анализа

Раньше каждый запрос спутника или прошлого сезона публиковал локальный процент и начинал шкалу заново. По БД проверенные jobs выполнялись с `attempt=1`: повторялась индикация, не весь запуск.

Теперь план включает все спутники и исторические сезоны. Кеш, пустые ответы, отказ источника и переходы между этапами не обнуляют прогресс. 100% появляется только после сохранения результата. Исторические данные имеют отдельное название этапа. Chart загружается во время расчёта; при публикации остаётся один этап подготовки результата. Повторное получение terminal state не вызывает повторный `onFinish` и invalidation при каждом рендере.

Семь новых Python-сценариев проверяют обе спутниковые системы, обычные/пустые/частично ошибочные ответы, кеш, границы доступной истории, 29 февраля и отмену. Новый браузерный сценарий задерживает ответ ряда и проверяет единственный переход от прогресса к готовому экрану без повторного запроса series; выполняется на desktop и mobile.

## Живой сквозной запуск

Выполнено:

```sh
uv run --frozen python scripts/api_smoke.py --reference-years 3 --output artifacts/jury-smoke
```

- Реальный контур Потсдама, период 01–10.06.2024, Sentinel-2 + Landsat + погода + три прошлых сезона.
- Job завершён с первой попытки. Получен `partial` с двумя днями пригодных наблюдений; ограничения сохранены в run.
- Все 39 зафиксированных состояний анализа подтверждают монотонный прогресс, финальное значение — 1.
- CSV, GeoJSON и JSON выгружены, manifests получены, SHA-256 проверены.
- 40 чтений при concurrency=4: p95 ≈41,64 мс, max ≈48,32 мс на небольшом прогретом локальном стенде. POST анализа ≈327,60 мс. Это проверка работоспособности, не SLA и не тест предельной нагрузки.

Повторная генерация submission в batch-контейнере дала 3 112 строк и SHA-256 `0fd8aebc5297d2e57d9244ca71e401660ac309d4cc2a1a0b2d595963530c02e1`, совпадающий с поставкой.

## Запуск из чистого клона

После подготовки публичного репозитория выполнена новая сборка из чистого Git checkout. Отдельный Compose-проект использовал новые volumes PostGIS и artifacts и отдельные локальные порты, без данных рабочего стенда.

Миграции и регистрация модели завершились автоматически. Frontend, readiness, создание гостевой сессии и чтение registry вернули HTTP 200. Проверены наличие session cookie/CSRF и активная модель `131aee618934151e`. Команды запуска из README не зависят от локальных рабочих материалов. Состав кода, датасеты и модель совпали с версией, прошедшей полный набор тестов и живой smoke.

## Что эта проверка не утверждает

На момент описанного первоначального сквозного запуска ответы для test не были предоставлены; метрики исследовательских отчётов относятся к указанным экспериментальным выборкам. Позднее выполнены локальная оценка новых вложений и проверка трёх реальных полей. Глобальная точность на произвольных территориях и нагрузочная пригодность не доказаны. Внешние провайдеры могут менять доступность данных. [Протокол модели](analysis/crop-dynamics/REPORT.md) и [маршрут ручной проверки](JURY_GUIDE.md) содержат условия воспроизведения.

## Дополнение: сверка исходных материалов

Семь вложений сопоставлены с исходниками и публикацией `9ea5be2`. Train, первый test и два PDF совпали побайтово; второй train-архив оказался дубликатом. Обнаружены два ранее неучтённых архива: другой тестовый вход и ответы к первому test. [Полный аудит](CASE_AUDIT.md), [машиночитаемый состав](analysis/input-review/materials.json).

- Неизменённый `deliverables/submission.csv` оценён на 3 112 точках: RMSE **0,0750342359**, MAE **0,0494095368**, GapScore **7,49 / 30**. Точное совпадение ключей подтверждено; это локальная оценка, не балл платформы.
- Из `test_features.csv` та же модель через Docker CLI построила 2 323 прогноза; отдельная `validate-submission` завершилась успешно. SHA-256 результата: `1fabb4ed5df3b8ed404e218de0e69b5ebeb38240ce0ea65c88a3bade4dfdc497`.
- Три новых теста скрипта оценки проверяют формулу, ZIP, несовпадающие ключи при одинаковом числе строк, дубли и неконечные прогнозы. Проверки Ruff пройдены. Этот аудит не является повторным полным прогоном приложения.
- Обучение и подбор параметров не выполнялись; исходные модель, manifest и submission сохранены с прежними хешами. Исходные ответы не опубликованы в Git.
