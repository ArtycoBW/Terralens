"use client";
import Link from "next/link";
import {usePathname} from "next/navigation";
import {Map,Layers3,ChartNoAxesCombined,ShieldCheck,BrainCircuit,FlaskConical,ArrowUpRight,Leaf,LogOut} from "lucide-react";
import {useWorkspace} from "./provider";
const nav=[["/app","Карта",Map],["/app/polygons","Мои поля",Layers3],["/app/compare","Сравнение",ChartNoAxesCombined],["/app/data-quality","Качество данных",ShieldCheck],["/app/models","Модели",BrainCircuit],["/app/benchmark","Бенчмарк",FlaskConical]] as const;
export function Shell({children}:{children:React.ReactNode}){const path=usePathname();const {session,reset}=useWorkspace();return <div className="workspace"><aside className="sidebar"><Link href="/" className="brand"><span className="brand-icon"><Leaf size={21}/></span>TerraLens<span className="version">BETA</span></Link><p className="nav-label">РАБОЧЕЕ ПРОСТРАНСТВО</p><nav aria-label="Основная навигация">{nav.map(([href,title,Icon])=><Link key={href} href={href} aria-current={(href==="/app"?path===href:path.startsWith(href))?"page":undefined}><Icon size={18}/><span>{title}</span></Link>)}</nav><div className="sidebar-bottom"><div className="session-card"><span className="status-dot"/>Гостевое пространство<small>Доступно до {new Date(session.expires_at).toLocaleDateString("ru-RU")}</small><button onClick={()=>{if(window.confirm("Завершить сессию? Доступ к сохранённым полям этого пространства будет потерян."))void reset();}}><LogOut size={14}/> Завершить сессию</button></div><Link href="/">О проекте <ArrowUpRight size={14}/></Link></div></aside><div className="workspace-content"><header className="workspace-header"><span>Обзор земной поверхности <span className="muted">/ Ретроспективный анализ</span></span><span className="workspace-tag"><span className="status-dot"/>Спутники → данные → решения</span></header><main id="main">{children}</main></div></div>}
