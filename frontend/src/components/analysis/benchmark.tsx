"use client";
import {useState} from "react";
import dynamic from "next/dynamic";
import data from "@/data/benchmark.json";
import {number} from "@/lib/api";
import {chartBase} from "@/lib/charts";
const Chart=dynamic(()=>import("./chart").then(m=>m.Chart),{ssr:false});
export function Benchmark(){const [id,setId]=useState(data.polygons[0]?.id||"");const p=data.polygons.find(p=>p.id===id);return <div className="page-pad"><div className="page-heading"><div><p className="eyebrow">Анонимный конкурсный набор</p><h1>Бенчмарк восстановления</h1><p className="small muted">Опубликованные предсказания только для скрытых целевых точек.</p></div><span className="pill">Модель {data.model_id}</span></div><div className="metrics"><div className="panel"><p className="metric-label">Целевых строк</p><p className="metric">{number(data.rows,0)}</p></div><div className="panel"><p className="metric-label">AOI с предсказаниями</p><p className="metric">{data.polygons.length}</p></div><div className="panel"><p className="metric-label">Географическая привязка</p><p className="metric text-lg mt-2">Отсутствует</p></div><div className="panel"><p className="metric-label">Официальный RMSE</p><p className="metric">—</p><p className="micro muted">Истинные скрытые цели недоступны</p></div></div><div className="notice mb-5">AOI анонимизированы и не показаны на карте. Предсказания не являются измерениями; расстояния между целевыми датами могут быть неравномерными.</div><section className="panel stack"><label className="field">Анонимный AOI<select value={id} onChange={e=>setId(e.target.value)}>{data.polygons.map(p=><option key={p.id} value={p.id}>{p.id} · {p.points.length} целей</option>)}</select></label>{p&&<><Chart height={330} option={{...chartBase,xAxis:{type:"time"},yAxis:{...chartBase.yAxis,name:"NDVI"},series:[{name:"Предсказанные цели",type:"scatter",symbolSize:6,itemStyle:{color:"#5df0a8"},data:p.points.map(p=>[p.date,p.value])}]}}/><div className="scroll-table max-h-[360px]"><table className="data-table"><thead><tr><th>Дата цели</th><th>primary_ndvi_pred</th></tr></thead><tbody>{p.points.map(p=><tr key={p.date}><td>{p.date}</td><td>{number(p.value,6)}</td></tr>)}</tbody></table></div></>}<p className="micro muted break-all">Источник: {data.source} · SHA-256: {data.sha256}</p></section><section className="panel mt-5 stack"><h2>Воспроизвести submission</h2><p className="small muted">CLI работает отдельно от веб-приложения; обучение и инференс выполняются в Python.</p><pre className="code-block">python -m terralens_ml predict --input test-dataset.csv --output submission.csv --model ml/artifacts/final/manifest.json{"\n"}python -m terralens_ml validate-submission --input test-dataset.csv --submission submission.csv</pre></section></div>}
